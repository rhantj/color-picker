# colorpicker

## 설명은 쉽게 한다

대화든 문서든 **먼저 쉬운 말로 쓰고, 정확한 용어는 그 뒤에 붙인다.** 이 저장소는 게이트 번호·
함수명·근거 등급 같은 내부 약속이 많아서, 그것들로만 말하면 **읽는 사람이 코드를 이미 아는
경우에만 통한다.** 그러면 설명이 아니라 참조다.

- **"무엇이 안 되나 · 그게 왜 문제인가" 를 먼저 쓴다.** 파일명·함수명·게이트 id 는 그다음이다.
  `S18-G3 이 잡았다` 보다 **`{ structureId: ["complementary"] } 가 그대로 통과했다`** 가 먼저다.
- **새 용어는 처음 나올 때 한 줄로 푼다.** 한 문단에 처음 보는 낱말이 둘 이상이면 쪼갠다.
- **표를 쓰면 "쉽게 말하면" 열을 둔다.** 항목 이름만 늘어놓은 표는 목록이지 설명이 아니다.
- **비유는 쓰되 그 자리에서 원래 말로 되돌린다.** 비유만 남으면 다음 사람이 코드에서 그것을
  못 찾는다.

**쉽게 쓴다고 부정확해지지 않는다.** 수치·근거 등급(`[실측]`·`[문헌]`·`[판단]`)·버린 대안은
그대로 남긴다. 줄일 것은 **전문 용어이지 근거가 아니다.**

## 계획 및 진행

- 모든 작업의 계획 수립과 진행 관리는 **`unlazy` 스킬**로 한다.
- 착수 전 완료 조건(gate)을 먼저 쓰고, 작업을 트리로 분해한 뒤 실행한다.
- 완료 보고 전에 증거를 재검증한다. 검증 없는 "완료" 보고 금지.
- **구현도 `unlazy` 로 진행한다.** 순서는 고정이다 — `GATES.md` 에 단계 절과 게이트를 적고 검사기
  (`scripts/check-stage<N>.mjs`)를 **구현보다 먼저** 써서 전부 실패하는 것을 본다 → 구현 → 게이트 전부
  통과 + 회귀 → 음성 대조(일부러 깨뜨려 게이트가 우는지) → 격리 리뷰 2인 → 지적 처리 후 재검증 →
  세션 재개 문서 · `open-work.md` → 대표 확인 뒤 커밋·머지. 설계가 있으면 `docs/superpowers/plans/`
  의 계획을 따른다.

## 문서 규칙

| 종류 | 위치 | 언제 쓰나 |
|---|---|---|
| 일반 문서 | `docs/com/` | 설계·명세·조사·가이드 등 상시 참조 문서 |
| 브레인스토밍 스펙 | `docs/superpowers/specs/` | `brainstorming` 스킬로 합의한 설계를 구현 착수 **전에** 고정할 때 |
| 세션 재개 문서 | `docs/session-resume/` | **하나의 작업이 끝날 때마다** 반드시 작성 |
| 트러블슈팅 | `docs/troubleshootings/` | 작업 중 문제가 생겼을 때 그 즉시 작성 |

### 브레인스토밍 스펙 (`docs/superpowers/specs/`)

- 파일명: `YYYY-MM-DD-<주제>-design.md` (`superpowers:brainstorming` 스킬의 기본 규약을 그대로 쓴다)
- `docs/com/` 과 나누는 기준은 **시점**이다. 여기 있는 문서는 코드가 아직 없는 상태에서 합의된
  것이고, 구현이 끝나 상시 참조 대상이 되면 `docs/com/` 으로 옮기거나 그쪽에서 새로 쓴다.
- 스펙을 쓴 뒤 구현 계획은 `writing-plans` 스킬로 이어 간다.

### 작업 목록 (`docs/com/open-work.md`)

**세션 재개 문서를 쓰는 그 순간에 이 파일도 함께 고친다.** 둘은 한 동작이다 — 작업이 끝나면
기록을 남기고(세션 재개), 남은 것을 다시 센다(작업 목록).

- 끝낸 항목은 **그 자리에서** 지우거나 취소선을 긋는다. "나중에" 는 없다.
- 새로 드러난 것은 그때 더한다. 리뷰가 찾은 것도 포함한다.
- 이 파일이 낡으면 **커밋되는 순간 거짓말이 된다** — 다음 사람이 이미 끝난 일을 다시 하거나,
  남은 일을 끝난 줄 안다.

**이 규칙은 세 번 놓친 뒤에 생겼다.** 18-A · 18-B · 19단계에서 연달아 낡은 채로 커밋될 뻔했고
세 번 다 리뷰가 잡았다. 두 번째 뒤에 그 파일 안에 경고를 적었는데도 세 번째가 났다 —
**경고문은 그 파일을 여는 사람만 읽는다.** 그래서 여기로 옮겼다.

### 세션 재개 문서 (`docs/session-resume/`)

- 파일명: `YYYY-MM-DD-<작업명>.md`
- 작업 하나가 끝나면 다음 세션이 맥락 없이도 이어받을 수 있게 남긴다.
  - 무엇을 했나 (변경된 파일과 이유)
  - 왜 그렇게 했나 (버린 대안 포함)
  - 지금 상태 (동작하는 것 / 안 되는 것)
  - 다음에 할 일

### 트러블슈팅 (`docs/troubleshootings/`)

- 파일명: `YYYY-MM-DD-<증상>.md`
- 해결한 문제만이 아니라 **막힌 채로 넘어간 문제도** 남긴다.
  - 증상 (에러 메시지 원문 포함)
  - 원인 (추정이면 추정이라고 명시)
  - 해결 방법 / 우회 방법
  - 재발 방지책 (없으면 "없음"이라고 적는다)
