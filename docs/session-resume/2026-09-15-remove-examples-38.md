# 2026-09-15 — 예시 칩 제거 · 빈 제출 무시 (38단계 · 대표 지시)

## 무엇을 했나

대표 지시: "코드 및 색상에서도 입력이 없는데 예시가 나오는 것도 전부 지워줘". "입력이 없는데 예시가 나온다" 를 두 경로로 읽었다.

1. **검색창 아래 "예시" 칩 줄** — 세 탭(색감 추천 5 · 캐릭터 3 · 코드 및 색상 3) 전부 뺐다. `public/index.html` 의 `.examples`
   블록, `public/app.js` 의 칩 표시·클릭 처리, `public/app.css` 의 `.examples`·`.chip` 규칙(터치 히트 영역 규칙 포함).
2. **빈 채로 제출하면 자리표시 문장을 대신 검색하던 동작** — 이제 아무것도 안 하고 검색창에 포커스만 준다.

자리표시 문장(placeholder) 자체는 남겼다 — 무엇을 넣는지 알려 주는 안내이고 입력이 없을 때 결과를 내지는 않는다.

## 게이트

새 게이트 없음. S34-G9·S35-G5 가 "예시 칩이 있다" 를 재고 있어 그 두 줄을 뺐다(지시로 칩이 사라졌다). 게이트 수 248 그대로.
회귀: S2 G1·G3·G5·G6·G8 · S31 G1~G3 · S33-G1 · S34-G9 · S35-G5 · S36-G4 · S22-G6 통과.

## 브라우저 실측 (Ollama 없이)

- 세 탭 모두 `.examples`·`.chip` 요소 0.
- 빈 검색창에서 제출 → `/api/*` 요청 0, 결과 영역 그대로, 포커스가 검색창.
- 글을 쓰고 제출 → 전과 같이 동작.

## 실측 뒤 혼선 하나

머지 뒤 대표가 "그대로 나온다" 며 결과가 찍힌 화면을 보였다. 원인은 코드가 아니라 **실측 중 열어 둔 옛 브라우저 탭** — 주소에
`?tab=color&q=#E07A5F` 가 붙어 있어 "다시 묻기" 경로로 자동 실행된 것이었다(상단 `0ms`). 서버가 내주는 HTML·JS 에 칩 0건을 다시
확인하고 옛 탭 셋을 닫았다. **실측이 끝나면 테스트 URL 이 붙은 탭을 닫는다** — 다음 사람이 그 화면을 코드 상태로 읽는다.

## 지금 상태

- **커밋 `6428a43` 로 `main` 에 올라갔다**(`stage-38-remove-examples` 브랜치, `--ff-only` 머지, 원격 `rhantj/color-picker`).
