# 2026-09-07 — 재질을 손으로 바꾼다 (21단계 · A3)

## 무엇이 안 됐나

로컬 LLM 이 **"본문은 광택"** 이라고 정하면 사용자는 **그대로 봐야 했다.**

```
바탕  무광     ← 읽기 전용. 바꿔 볼 수가 없었다
본문  광택
강조  메탈릭
```

배정이 마음에 안 들어도 할 수 있는 것이 없었다. 다시 물어봐도(질문을 바꿔 다시 검색해도)
같은 답이 나올지 알 수 없고, 그러면 **보이는 다섯 배색이 통째로 바뀐다.**

## 무엇을 만들었나

역할마다 고르개가 생겼다.

```
바탕  [무광 (LLM 배정) ▾]
본문  [광택 (LLM 배정) ▾]   ← 여기서 발광으로 바꾸면
강조  [메탈릭 (LLM 배정) ▾]
```

바꾼 것이 **저장에 남고 엔진 수치까지** 간다. 본문을 발광으로 바꾸면 언리얼 파일에
`emissiveColor: #de2143 · emissiveIntensity: 2` 가 나온다(실측).

### 되돌릴 길을 상태 없이 만들었다

이것저것 바꿔 본 뒤 원래로 돌아가려면 **어느 것이 원래였는지** 알아야 한다.
되돌리기 버튼과 그 상태를 따로 두는 대신, **목록 안에 적었다** — LLM 이 고른 항목에만
`(LLM 배정)` 이 붙는다. 상태가 하나도 안 늘어난다.

## 함정 셋을 피해야 했다

이 단계는 새 기능이라기보다 **세 가지 함정을 피하는 일**이었다.

### 1. 고친 재질이 모드 토글에 날아간다

모드 토글은 격자를 **통째로 다시 그린다**(`redraw`). 고르개를 카드 안에 그냥 넣으면
어두운 모드로 바꾸는 순간 **사용자가 고친 것이 전부 사라진다.**

15단계에서 겪은 것과 같은 부류다(그때는 포커스가 사라졌다).

**바꾼 배정을 카드 밖 — 펼침 영역 클로저에 뒀다.** `S21-G4` 가 그 자리를 정적으로 검사한다:
배정 상태가 `redraw` 보다 **위**에 선언돼 있는가.

```
실측:  본문을 발광으로 바꾼다 → 어두운 모드로 토글 → 여전히 발광
```

### 2. 한 카드를 고쳤는데 일곱이 같이 바뀐다

배정 표는 **역할별로 하나**다(`본문 → 광택`). 카드 여덟이 그것을 함께 본다.
그대로 고치면 **안 건드린 카드 일곱이 조용히 달라진다.**

**구조마다 따로 담는다.** 사용자는 하나의 배색을 다듬는 중이고, 저장도 구조 단위다.

```
실측:  첫 카드의 본문을 바꿈 → 나머지 네 카드 그대로
```

### 3. 바꾸려고 서버를 다시 부른다

`/api/expand` 를 다시 부르면 `selectStructures` 가 다시 돌아 **같은 질의인데 보이는 다섯이
바뀐다.** 재질을 하나 고치려다 화면 전체가 갈리는 것이다 — 15단계가 모드 토글에서 피한
것과 정확히 같은 함정이다(`S15-G11`).

담아 두기만 한다. 네트워크 0.

### 4. 다시 그리기만 해도 망가진다 (구현 중에 스스로 찾음)

**함정 셋을 피해 놓고 네 번째에 걸렸다.** 서버는 안 불렀지만 **격자를 다시 그렸고**,
그것이 브라우저 실측으로 두 가지를 망가뜨렸다.

```
① 사용자가 맞춘 면적 비율    55 : 25 : 20  →  34 : 33 : 33   (조용히 초기화)
② 방금 조작한 고르개의 포커스  select       →  body           (사라짐)
```

**①이 더 나쁘다.** 이 사이트는 "면적이 색의 일부다 · 같은 두 헥스도 비율이 바뀌면 다른 색"
이라고 말해 왔다. 비율을 맞추고 재질을 하나 고쳤을 뿐인데 그 작업이 사라지면, 화면이
스스로의 주장을 배신한다.

②는 모드 토글이 **이미 겪고 `focus()` 로 막아 둔** 문제다. 그 주석을 내 코드에서 인용해
놓고 같은 실수를 했다.

**애초에 다시 그릴 필요가 없었다.** 고르개는 사용자가 고른 값을 이미 스스로 보이고 있고,
카드에서 재질에 딸린 것은 그것 하나뿐이다 — 스와치·비율·`(LLM 배정)` 표시는 전부 재질과
무관하다(표시는 **원래** 배정 기준이라 안 바뀐다). 그냥 담아 두기만 하면 된다.

```
고친 뒤:  비율 55:25:20 그대로 · 포커스가 그 고르개에 그대로 · 요소도 DOM 에 살아 있음
모드 토글: 바꾼 재질이 여전히 남음 (합치기는 forStructure 가 그때 한다)
```

`S21-G4` 가 이 회귀를 막는다 — **`onFinish` 안에 `redraw` 가 있으면 운다.**

**교훈: "서버를 안 부른다" 는 "화면을 안 부순다" 와 다른 말이다.** 앞의 함정 셋은 전부
바깥(서버·다른 카드·모드)을 건드리는 것이었고, 이것은 **자기 카드 안**을 부수는 것이라
같은 눈으로는 안 보였다.

## 파일

| 파일 | 무엇 |
|---|---|
| `public/ui.js` | 순수 함수 둘 — `finishOptions`(목록) · `finishOverrides`(바꾼 것 담기) · `structureCard` 에 고르개 |
| `public/app.js` | 배정 상태를 `redraw` 밖에 두고 저장에 잇는다. 고칠 때는 **담아 두기만** 한다 |
| `public/app.css` | `.struct__finish-pick` — 읽기 전용 표시와 **줄 높이가 같아야** 카드 여덟이 안 밀린다 |
| `scripts/check-stage21.mjs` | S21-G1~G7 |
| `GATES.md` · `README.md` · `docs/com/open-work.md` | 게이트 163 → 170 |

**순수 함수로 뺀 이유는 게이트다.** 화면 코드는 정적 검사밖에 못 하는데, 정적 검사는
"바꿨을 때 무엇이 남는가" 를 못 본다. 17단계 `structureColors` · 18단계 `savedFields` ·
20단계 `engineToggle` 과 같은 자리다.

## 게이트 (실측)

**170개 전부 통과** (기존 163 + S21-G1~G7). 기존 게이트 회귀 0.

```
S21-G1  고르개가 재질 넷을 한글 이름으로 주고 LLM 배정에만 표시가 붙는다
S21-G2  바꾼 배정이 구조마다 따로 살고, 모르는 재질·역할이 안 들어간다
S21-G3  카드가 역할마다 고르개를 그리고, 지금 배정으로 열리고, 고치면 알린다
S21-G4  배정 상태가 redraw 밖에 있고 카드가 합쳐진 배정을 받는다
        고칠 때 서버를 안 부르고 카드를 다시 그리지도 않는다 (비율·포커스가 날아간다)
S21-G5  저장 요청이 바꾼 배정을 싣고, 색은 여전히 안 보낸다
S21-G6  손으로 바꾼 재질이 저장에 그대로 남는다 — 저장소를 직접 불러 끝에서 끝까지
S21-G7  ui.js 에 HTML 문자열 조립이 없고, 재질 이름이 글자로만 들어간다
```

### 브라우저가 없어서 DOM 을 흉내 냈다

`structureCard` 가 무엇을 그리는지는 **부르지 않고는 알 수 없다.** 의존성 0 저장소라
jsdom 을 안 들이고, 그 함수가 실제로 쓰는 것만 만들었다 — `append` · `textContent` ·
`setAttribute` · `addEventListener` · `style`.

**일부러 더 안 만들었다.** 흉내가 브라우저와 어긋나는 자리는 게이트가 못 보는데,
더 만들수록 그런 자리가 늘고 아무도 그 어긋남을 안 본다.

### 뮤테이션 17개 — 처음에 셋이 살아남았다

| 살아남은 변형 | 무엇이 문제였나 |
|---|---|
| 이름 조회에서 자기 속성 검사를 뺀다 | 재질 id 가 `constructor` 같은 것이면 이름표에서 **함수**가 나와 화면에 찍힌다. 게이트가 그 경우를 안 봤다 |
| 원래 배정 조회에서 자기 속성 검사를 뺀다 | 서버가 보낸 배정에 화면이 모르는 재질(`velvet`)이 있으면 그대로 실린다 — **사용자는 그것으로 저장했다고 믿는데** 서버가 거르고 기본값이 앉는다 |
| `redraw` 가 바꾼 배정을 안 읽는다 | **게이트가 엉뚱한 데를 봤다.** 아래 참조 |

셋 다 메우고 다시 돌려 **17개 전부 잡힌다.**

### 게이트가 한 번 뚫린 자리

`S21-G4` 는 처음에 `redraw` 본문에 `overrides` 라는 글자가 있는지만 봤다. **그걸로는
못 잡는다** — 카드에 넘길 것을 만드는 자리가 `redraw` **밖**에 있고, 거기서 배정을
원래 것으로 바꿔치기해도 `overrides.set` 이 남아 검사가 통과했다.

지금은 **`assignments:` 에 실리는 값이 `forStructure` 를 거치는지**를 본다. 합치는 규칙이
한 곳에만 있으므로, 그것을 안 거치면 합쳐진 것일 수 없다.

19단계 `S19-G5` 가 겪은 **"조립부와 호출부가 따로 있다"** 와 같은 부류다. 세 번째다.

## 브라우저 실측

```
고르개                      25개 (카드 5 × 역할 3~4, 나머지 접힌 것 포함)
목록                        무광 (LLM 배정) · 광택 · 메탈릭 · 발광
읽어 주는 이름              "바탕의 재질" (역할을 붙여야 어느 자리인지 안다)
첫 카드 본문 → 발광          바뀜
  나머지 네 카드            안 바뀜
  어두운 모드로 토글         발광 그대로 살아남음
비율 55:25:20 맞춘 뒤 재질 변경
  비율                      55:25:20 그대로 (다시 그리기를 뺀 뒤)
  포커스                    그 고르개에 그대로 · 요소도 DOM 에 살아 있음
저장                        finishes.본문 = emissive · finishesAdjusted = true
언리얼 내보내기              emissiveColor #de2143 · EV 2
고르개 대비                 7.92:1
고르개 높이                 26px (읽기 전용 표시와 줄 높이 같음)
375px 가로 스크롤            0 · 재질 줄 넘침 0
```

**스크린샷은 못 남겼다.** 렌더 타임아웃으로 계속 빈 화면이 잡혔다 — 수치 확인으로 대신했다.

## 지금 상태

- **A3 이 닫혔다.** 화면에서 재질을 고르고, 저장하고, 엔진 수치로 받는 길이 끝까지 이어진다.
- 게이트 170/170 · 회귀 0 · 변형 17개 전부 잡힘.

## 다음에 할 일

**A 묶음에 둘 남았다.**

1. **22단계 — `/saved` 가 저장된 재질을 보여준다.** 저장에도 남고 내보내기로도 나가는데
   **목록에서만 안 보인다.** `savedFields` 가 재질을 정규화해 내보내면 된다.
2. **A4 — 새로고침하면 어두운 모드가 풀린다. 결정이 먼저다.**
   `public/app.js` 가 *"전역이나 localStorage 에 두지 않는다 — 카드마다 다른 모드로 나란히
   비교할 수 있고, 이 저장소에 없던 저장 계층을 들이지도 않는다"* 를 **명시적 결정으로**
   적어 뒀다. 뒤집으려면 그 근거를 다시 봐야 한다.

   합치는 길이 있기는 하다 — **기본 모드만 저장하고 카드별 어긋남은 그대로 두는 것.**
   그러면 "나란히 비교" 도 살고 새로고침도 견딘다. 다만 저장 계층을 들이는 것은 그대로다.

## 열려 있는 것

- **`/saved` 에서는 재질을 못 바꾼다.** 홈에서만 된다. 22단계가 보여주기까지 하면, 거기서도
  고칠지는 따로 정할 일이다 — 저장된 것을 고치는 것은 새 저장이 아니라 수정이라 경로가 다르다.
- **`S21-G4`·`S21-G5` 는 정적 검사라 표기만 본다.** 다른 변수로 조립해 우회할 수 있다.
  실질 방어는 순수 함수 쪽이고 `S21-G2`·`S21-G6` 이 그것을 직접 부른다.
- **흉내 낸 DOM 이 브라우저와 어긋나는 자리는 게이트가 못 본다.** 실제 확인은 손으로 한다.
