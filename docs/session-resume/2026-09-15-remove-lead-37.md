# 2026-09-15 — 홈 소개 문장 제거 (37단계 · 대표 지시)

## 무엇을 했나

대표 지시: 홈 제목 아래 "배색사전 16쌍과 진단표 18건을 문장으로 찾습니다. 색 조합을 물으면 조합을, 왜 그런지 물으면 의심할
원인을 답합니다." 문장을 뺀다. `public/index.html` 의 `.hero__lead` 문단과 `public/app.css` 의 그 규칙(다른 곳에서 안 씀)을 지웠다.

## 게이트

새 게이트 없음(문단 하나 삭제). **S33-G1 이 울었다** — 33단계가 "홈 소개 문장에 '의심할 원인' 이 있다" 를 걸어 뒀기 때문이다.
지시로 그 문장 자체가 사라졌으므로 검사를 진단 결과의 안내줄(`app.js` "어느 원인을 의심할지가 답입니다")로 옮겼다. ui.js 의
"의심할 원인" 검사는 그대로다. 게이트 수 248 변화 없음.

회귀: S2 G1·G5·G6·G8 · S31 G1·G3 · S32 G1·G2 · S33 G1~G3 · S34-G9 · S35-G5 · S36-G4 통과.

## 브라우저 실측

`.hero__lead` 요소 없음, 제목 아래 바로 탭(제목 하단 878 → 탭 상단 914, 36px 간격 그대로).

## 지금 상태

- **커밋 `c850d15` 로 `main` 에 올라갔다**(`stage-37-remove-lead` 브랜치, `--ff-only` 머지, 원격 `rhantj/color-picker`).
