# 2026-09-13 — 화면에서 검색 사다리와 "LLM 이 …" 문구를 뺀다 (31단계 · 대표 지시)

## 무엇을 했나

대표 지시: "사이트 밑의 단계가 나오는 거랑 LLM 이 추천했다는 문구를 지워줘". LLM 을 말하는 자리가 셋이라 물었고,
셋 다 지우기로 했다.

| 자리 | 전 | 후 |
|---|---|---|
| 화면 아래 | 검색 구조 사다리 "1 전문검색 … 6 전체 사전임베딩 · 지금은 N단계" | 없음 |
| 결과 위 | "1단계 · 전문 검색이 바로 찾았습니다. LLM 을 부르지 않았습니다" 같은 배지 + 문장 | 없음. 시간 줄("BM25 41ms · 2건")과 저신뢰 경고는 그대로 |
| 맨 위 소개 | "임베딩도 벡터DB도 쓰지 않습니다 … 로컬 LLM 이 질문을 검색어로 바꿉니다" (이미 사실과 달랐다) | "배색사전 16쌍과 진단표 18건을 문장으로 찾습니다. 색 조합을 물으면 조합을, 왜 그런지 물으면 의심할 축을 답합니다." |
| 펼친 카드 | "LLM 이 고름 / 기본 순서" · "LLM 이 배정 / 기본 배정" 배지와 설명 문장 | 없음 |
| 재질 고르개 | `광택 (LLM 배정)` | `광택 (처음 값)` — 되돌릴 자리를 알려 주는 표시라 이름만 바꿔 남겼다(21단계 결정 유지) |
| 홈 대화 메타 | "N턴 · LLM n회" | "N턴 · 재작성 n회" — 무엇을 했는지로 적는다 |

서버 · 기록(`usedLlm`) · 대화 내역 화면(`history.js`)은 안 건드렸다. 화면 문구만이다.

## 게이트

3개 신설(218 → 221). 구현 전 G1·G2 실패, G3 는 회귀라 통과 → 구현 후 전부 통과.

| 게이트 | 무엇 |
|---|---|
| S31-G1 | 정적 — 사다리·LLM·단계 배지가 **없고**, 검색 폼·시간 줄·`usedLlm` 은 **있다**(파일을 비워도 통과하면 안 된다) |
| S31-G2 | 서버가 내주는 홈 + `finishOptions` 의 `(처음 값)` 표시가 처음 배정에만 |
| S31-G3 | S2-G1 · S14-G7 · S21-G1 · S26-G7 · S27-G7 — 뒤집힌 판정으로 통과 |

옛 게이트 넷을 뒤집었다: S2-G1(홈에 `ladder` 필요 → 제외), S14-G7("LLM 이 고름" 있어야 → 없어야), S21-G1·G3(`LLM`
표시 → `처음 값`), S26-G7(3단계 배지·사다리 검사 제거, 기록 쪽만). 화면 회귀 78개(S2·S14·S17·S18·S21·S22·S23·S26·S27·S31)
통과.

## 게이트가 못 잡은 결함 `[실측]`

배지 블록을 지우다 `const fin = data.finishes` 까지 지웠다. "펼치기" 가 `fin is not defined` 로 죽었는데 게이트
78개가 전부 통과했다 — S14·S17·S18·S21 은 `structureCard` 를 DOM 스텁으로 직접 부르지 `app.js` 의 클릭 핸들러를
안 탄다. 브라우저에서 잡았다. **문구를 지우는 변경은 "없다" 만 재기 쉽고, "지운 자리 뒤가 아직 돈다" 는 실제
화면에서만 안다.** `GATES.md` 알려진 한계에 적었다.

## 브라우저 실측 (localhost:4173)

- 홈: 사다리 없음 · 소개 문장 교체 · `LLM` 글자 0.
- "청량한 여름 화장품 브랜드" → 결과 위에 "BM25 36ms · 2건" 만. 배지 0.
- 펼치기 → 카드 8장 · 배지 0 · 고르개 `무광 (처음 값)` 등 · `LLM` 글자 0.

## 잃은 것

폴백인지(LLM 이 못 골라 카탈로그 순서인지)를 화면에서 알 길이 없어졌다. 대표 지시의 결과다. API 응답의
`selection.from` · `finishes.from` 과 기록은 그대로라 필요하면 거기서 본다.

## 격리 리뷰 두 건

라운드 49, `typescript-reviewer` · `code-reviewer`. Critical 0.

### typescript-reviewer — P1 1 · P2 3

| 지적 | 처리 |
|---|---|
| **P1 — 비운 `expand__note` 가 성공 경로에서도 붙어 12px 여백을 남긴다** | **고쳤다.** 성공 경로는 `modeBox`·`grid` 만 붙인다. 실패 경로는 "펼치는 중…" 때 붙인 note 에 문장을 쓴다. 브라우저 실측: 펼친 카드 안에 `.expand__note` 없음 |
| P2 — `.expand__note-text` 등 죽은 CSS 를 S14-G7 이 강제한다 | **고쳤다.** `finNote` 와 CSS 세 규칙(`expand__note-text`·`expand__finish-note`·`expand__finish-text`)을 지우고 S14-G7·S17-G12 를 뒤집었다 |
| P2 — `stripJs` 가 문자열 안 `//` 를 주석으로 오인할 수 있다 | 안 고쳤다. 지금 화면 코드에 그런 문자열이 없고(리뷰어도 확인), 26단계 검사기와 같은 정규식이다. 알려진 한계로 둔다 |
| P2 — `index.html` 빈 줄 둘 | 고쳤다 |

### code-reviewer — P1 1 · P2 1

| 지적 | 처리 |
|---|---|
| **P1 — `GATES.md` 21·26단계 절 본문이 옛 화면(`(LLM 배정)` · 3단계 배지 · 사다리)을 사실처럼 서술한다** | **고쳤다.** 세 줄에 "31단계에서 뒤집음" 을 덧붙였다 |
| P2 — `finNote` 가 늘 죽어 있다 + CSS 세 규칙 | 위와 같이 지웠다 |

확인해 준 것: 새 소개 문장의 수치(16쌍 · 18건)가 코퍼스와 맞음 · 저신뢰 경고·`rewriteError`·`hybridError`·시간 줄·재작성 스트립은 남아 정보 손실 없음 · `refreshRuntime` 의 빈 콜백이 `ui.js` 계약과 맞음 · `fin` 재발 없음.

지적 처리 뒤 화면 회귀 78개(S2·S14·S17·S18·S21·S22·S23·S26·S27·S31)를 다시 돌려 전부 통과했다. 게이트 221 네 곳 일치(S22-G6).

## 지금 상태

- 브랜치 `stage-29-30-aliases-and-embed-keepalive` 에 29·30·31 이 함께 있다. 커밋 전.

## 확인 질문

1. 재질 고르개의 `(처음 값)` 은 왜 지우지 않고 이름만 바꿨나요?
2. 게이트 78개가 통과했는데 펼치기가 죽었던 이유는 무엇이고, 그런 변경은 무엇으로 확인해야 하나요?
